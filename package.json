{
  "name": "number-management-service",
  "version": "1.0.0",
  "description": "Need to develope HTTP microservice that exposes a GET REST API, which recieves a list of URLs through query parameters.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "keyword1",
    "keyword2"
  ],
  "author": "Lakshit Jain",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.0",
    "express": "^4.18.2"
  }
}
